import { radioButtons } from 'enlightened-citizenry/helpers/radio-buttons';
import { module, test } from 'qunit';

module('Unit | Helper | radio buttons');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = radioButtons([42]);
  assert.ok(result);
});
